<mvc:View xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    controllerName="eagleburgmann.training.orders.ext.main.Main">
    <Page id="Main">
        <customHeader>
        <Toolbar id="ActionToolbar" class="sapUiSmallMarginTop">
            <Title text="{i18n>orderDetails}" level="H2"/>
            <ToolbarSpacer/>
            <Button id="EditButton" text="Edit" type="Emphasized" press=".onEditPress"/>
        </Toolbar>
    </customHeader>

        <content>
            <f:SimpleForm
                id="OrderForm"
                editable="false"
                layout="ResponsiveGridLayout"
                labelSpanXL="3" labelSpanL="3" labelSpanM="3" labelSpanS="12"
                adjustLabelSpan="false"
                emptySpanXL="4" emptySpanL="4" emptySpanM="4" emptySpanS="0"
                columnsXL="1" columnsL="1" columnsM="1"
                singleContainerFullSize="false">

                <Label text="Order ID"/>
                <Input value="{ID}" editable="false"/>

                <Label text="User ID"/>
                <Input value="{user_ID}" editable="false"/>

                <Label text="Product ID"/>
                <Input value="{product_ID}" editable="false"/>

                <Label text="Quantity"/>
                <Input value="{quantity}" editable="false"/>


                <Label text="Order Date"/>
                <Input value="{orderDate}" editable="false"/>
                

                <Label text="Shipping Address ID"/>
                <Input value="{shippingAddress_ID}" editable="false"/>
            </f:SimpleForm>
        </content>

        <footer>
            <Toolbar id="FooterToolbar">
                <ToolbarSpacer/>
                <Button id="SaveButton" text="Save" type="Accept" press=".onSavePress" enabled="false"/>
                <Button id="DiscardButton" text="Discard" type="Reject" press=".onDiscardPress"/>
            </Toolbar>
        </footer>
    </Page>
</mvc:View>
